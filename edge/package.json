{
  "name": "kepler-edge",
  "version": "0.1.0",
  "private": true,
  "description": "KEPLER edge layer: Hono on Vercel — R2 uploads, Modal pipeline relay, Claude verdict streaming.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "typecheck": "tsc --noEmit",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "@aws-sdk/client-s3": "^3.699.0",
    "@aws-sdk/s3-request-presigner": "^3.699.0",
    "hono": "^4.6.14",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vercel": "^39.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
